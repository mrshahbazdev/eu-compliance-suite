# Translations

The plugin ships English and German strings inline via `__()` / `_e()`. Place
compiled `.mo` / `.po` / `.json` files here for additional locales.

Filename convention: `eurocomply-vat-oss-<locale>.mo` (e.g. `eurocomply-vat-oss-de_DE.mo`).
