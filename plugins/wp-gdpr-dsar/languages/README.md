# Translations

Generate a `.pot` file with WP-CLI:

```
wp i18n make-pot plugins/wp-gdpr-dsar plugins/wp-gdpr-dsar/languages/eurocomply-dsar.pot
```

The plugin uses the `eurocomply-dsar` text domain.
