# Translations

Place compiled `.mo` / `.po` files here with the textdomain prefix `eurocomply-cookie-consent-{locale}`.

Default banner copy for English and German is shipped inline via `Settings::default_text_en()` / `default_text_de()`; additional locales can be added by translating those arrays through WordPress `__()` calls.
